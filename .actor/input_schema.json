{
  "title": "Airbnb Listings Finder Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "location": {
      "title": "Location",
      "type": "string",
      "description": "Location to search for Airbnb listings (e.g., 'Miami, FL', 'New York, NY')",
      "editor": "textfield",
      "example": "Miami, FL"
    },
    "numberOfListings": {
      "title": "Number of Listings",
      "type": "integer",
      "description": "Maximum number of listing URLs to extract",
      "editor": "number",
      "default": 10,
      "minimum": 1,
      "maximum": 100
    }
  },
  "required": ["location", "numberOfListings"]
}
